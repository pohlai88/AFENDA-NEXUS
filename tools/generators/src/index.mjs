#!/usr/bin/env node
/**
 * @afenda/generators — Scaffolding generator for new packages/modules.
 *
 * Usage:
 *   node tools/generators/src/index.mjs --type library --name my-pkg
 *
 * Generates:
 *   - package.json with exports map + catalog deps
 *   - tsconfig.json + tsconfig.build.json
 *   - tsup.config.ts
 *   - eslint.config.js
 *   - src/index.ts stub
 *   - ARCHITECTURE.*.md with frontmatter
 *   - Updates .afenda/project.manifest.json
 */

console.log("@afenda/generators -- stub. Run with --help for usage.");
