/**
 * Consolidation sub-domain layout.
 *
 * Wraps all routes under /finance/consolidation/*.
 * Use this to add consolidation-specific providers, breadcrumbs, or sub-navigation.
 */
export default function ConsolidationLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
