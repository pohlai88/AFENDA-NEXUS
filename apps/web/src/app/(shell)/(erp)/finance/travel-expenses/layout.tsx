/**
 * Travel & Expenses sub-domain layout.
 *
 * Wraps all routes under /finance/travel-expenses/*.
 * Use this to add expense-specific providers, breadcrumbs, or sub-navigation.
 */
export default function TravelExpensesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
