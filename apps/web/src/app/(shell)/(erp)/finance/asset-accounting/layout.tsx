/**
 * Asset Accounting sub-domain layout.
 *
 * Wraps all routes under /finance/asset-accounting/*.
 * Use this to add asset-specific providers, breadcrumbs, or sub-navigation.
 */
export default function AssetAccountingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
