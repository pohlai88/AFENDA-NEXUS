/**
 * Banking & Liquidity sub-domain layout.
 *
 * Wraps all routes under /finance/banking-liquidity/*.
 * Use this to add banking-specific providers, breadcrumbs, or sub-navigation.
 */
export default function BankingLiquidityLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
