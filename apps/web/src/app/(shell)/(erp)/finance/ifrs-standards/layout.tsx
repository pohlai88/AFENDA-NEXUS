/**
 * IFRS Standards sub-domain layout.
 *
 * Wraps all routes under /finance/ifrs-standards/*.
 * Use this to add IFRS-specific providers, breadcrumbs, or sub-navigation.
 */
export default function IfrsStandardsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
