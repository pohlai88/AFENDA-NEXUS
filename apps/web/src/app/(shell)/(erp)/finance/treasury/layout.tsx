/**
 * Treasury sub-domain layout.
 *
 * Wraps all routes under /finance/treasury/*.
 * Use this to add treasury-specific providers, breadcrumbs, or sub-navigation.
 */
export default function TreasuryLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
