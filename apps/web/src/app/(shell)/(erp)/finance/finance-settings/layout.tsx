/**
 * Finance Settings sub-domain layout.
 *
 * Wraps all routes under /finance/finance-settings/*.
 * Use this to add settings-specific providers, breadcrumbs, or sub-navigation.
 */
export default function FinanceSettingsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
