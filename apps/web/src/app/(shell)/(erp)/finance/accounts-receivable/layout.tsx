/**
 * Accounts Receivable sub-domain layout.
 *
 * Wraps all routes under /finance/accounts-receivable/*.
 * Use this to add AR-specific providers, breadcrumbs, or sub-navigation.
 */
export default function AccountsReceivableLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
