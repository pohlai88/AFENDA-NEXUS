/**
 * Financial Reports sub-domain layout.
 *
 * Wraps all routes under /finance/financial-reports/*.
 * Use this to add reports-specific providers, breadcrumbs, or sub-navigation.
 */
export default function FinancialReportsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
