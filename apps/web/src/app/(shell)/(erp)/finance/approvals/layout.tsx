/**
 * Approvals sub-domain layout.
 *
 * Wraps all routes under /finance/approvals/*.
 * Use this to add approval-specific providers, breadcrumbs, or sub-navigation.
 */
export default function ApprovalsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
