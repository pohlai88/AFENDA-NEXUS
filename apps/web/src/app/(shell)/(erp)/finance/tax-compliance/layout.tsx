/**
 * Tax & Compliance sub-domain layout.
 *
 * Wraps all routes under /finance/tax-compliance/*.
 * Use this to add tax-specific providers, breadcrumbs, or sub-navigation.
 */
export default function TaxComplianceLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
